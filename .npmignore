# native/include/ and native/prebuilds/ are downloaded at install time (postinstall).
# They must NOT be in the npm package — they are platform-specific and generated.
native/include/
native/prebuilds/

# Exclude test, scripts, and other dev files
test/
scripts/
# Exclude build artifacts
build/
# Exclude config and dotfiles not needed in package
*.log
*.tsbuildinfo
*.swp
*.swo
*.DS_Store
# Exclude local env files
.env*
# Exclude benchmarks
benchmarks/
# Exclude editor folders
.vscode/
.idea/
# Exclude coverage
coverage/
# Exclude reports
reports/
