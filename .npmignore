# native/include/ and native/prebuilds/ are intentionally included in the npm
# package so that the postinstall "prebuilt-shipped" path works without any
# compilation, and the "hybrid" path has headers available for node-gyp.

# Exclude test, scripts, and other dev files
test/
scripts/
# Exclude build artifacts
build/
# Exclude config and dotfiles not needed in package
*.log
*.tsbuildinfo
*.swp
*.swo
*.DS_Store
# Exclude local env files
.env*
# Exclude benchmarks
benchmarks/
# Exclude editor folders
.vscode/
.idea/
# Exclude coverage
coverage/
# Exclude reports
reports/
